apiVersion: v1
kind: Service

metadata:
  name: mysql
  namespace: {{ .Release.Namespace }}

spec:
  selector:
    app: mysql

  ports:
    - port: {{ .Values.mysql.service.port }}
      targetPort: {{ .Values.mysql.service.port }}

  type: ClusterIP